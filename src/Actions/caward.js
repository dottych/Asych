module.exports = (params, res) => {
    // caward stands for Championship Award

    res.end(require('../Utils').response({
        
        return: "newpms",
        i: 0, // invites
        n: 0, // new PMs count
        b: 0 // banned

    }));

    // stub, and are we sure this is what it should return?
}