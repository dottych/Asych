const port = 80;

const dataProcUrl = "/dataproc/d1_1_3.asp";

const defaultScore = 0; // XP
const defaultMoney = 10000; // track credits

module.exports = { port, dataProcUrl, defaultScore, defaultMoney }